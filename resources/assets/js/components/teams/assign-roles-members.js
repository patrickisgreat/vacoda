Vue.component('assign-roles-members', {
    props: ['user', 'team'],

    /**
     * The component's data.
     */
    data() {
        return {
            form: new SparkForm({

            })
        };
    },

    /**
     * Prepare the component.
     */
    ready() {
    },

    methods: {
        /**
         * Update the team details.
         */
        update() {
        }
    }

});